apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newrelic.fullname" . }}-test-connection"
  labels:
    {{- include "newrelic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "newrelic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
